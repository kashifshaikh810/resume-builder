import React from "react";
import AccountSettingsMarkup from "./AccountSettingsMarkup";

const AccountSettings = (props) => {
  return <AccountSettingsMarkup {...props} />;
};

export default AccountSettings;
